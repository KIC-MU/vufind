<?php
$encodings = [];
foreach ($this->driver->getEncodings() as $encoding) {
  $encodings[] = $this->escapeHtml($encoding);
}
?>
<?php if (!empty($encodings)): ?>
  <?=implode("<br />", $encodings)?>
<?php endif; ?>
